-- Módulo 3: Tela
entity Tela is
  Port (
    -- Entradas
    clk, reset : in  std_logic;
    total      : in  integer range 0 to 9999;
    
    -- Saídas
    -- Aqui você pode implementar a lógica para exibir o total na tela (LCD, monitor, etc.)
  );
end Tela;

architecture Comportamental_Tela of Tela is
begin
  process (clk, reset)
  begin
    if reset = '1' then
      -- Lógica para reinicializar a tela
    elsif rising_edge(clk) then
      -- Lógica para exibir o total na tela
    end if;
  end process;

end Comportamental_Tela;